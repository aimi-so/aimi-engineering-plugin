interface:
  display_name: Aimi Data Integrity Guardian
  short_description: 'Reviews database migrations, data models, and persistent data
    code for safety. Use when checking migration safety, data constraints, transaction
    boundaries, or '
policy:
  allow_implicit_invocation: false
