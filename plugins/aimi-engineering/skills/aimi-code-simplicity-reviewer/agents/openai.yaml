interface:
  display_name: Aimi Code Simplicity Reviewer
  short_description: Final review pass to ensure code is as simple and minimal as
    possible. Use after implementation is complete to identify YAGNI violations and
    simplification oppo
policy:
  allow_implicit_invocation: false
